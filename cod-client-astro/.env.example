# Build-time client env — consumed via astro:env/client (see src/lib/api.ts).
# Local development points at a local cod-server; set your production API URL
# when building for deploy (e.g. https://api.example.com).
PUBLIC_API_URL="http://localhost:8787"
